{
  "name": "stuff-quiz",
  "version": "1.0.0",
  "description": "Calls a webhook with latest stuff quiz scraped from the quizzes page",
  "main": "index.js",
  "scripts": {
    "zip-code": "zip -r deploy.zip ./quizScraper.js ./quizPostHandler.js lib blocks ./node_modules -x 'node_modules/aws-sdk/*'",
    "deploy": "npm run zip-code && node ./deploy.js",
    "test": "jest",
    "test:ci": "jest --ci --runInBand --reporters=default --reporters=jest-junit --collectCoverage=true"
  },
  "author": "Michael Lowe",
  "license": "ISC",
  "dependencies": {
    "@slack/web-api": "^5.7.0",
    "aws-sdk": "^2.633.0",
    "axios": "^0.19.0",
    "cheerio": "^1.0.0-rc.3",
    "dotenv": "^8.2.0",
    "jest-junit": "^10.0.0"
  },
  "devDependencies": {
    "jest": "^25.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
